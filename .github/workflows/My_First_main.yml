name: my-first-workflow
#To get the workflow triggeered at every push
on: 
 push:
jobs:
  job1:
    name: JOB1
    runs-on: ubuntu-latest
    
    steps:
      - name: Running single command in JOB1
        run: echo "Hi this is Hemant's workdlow"
      - name: check1
        run: |
          pwd
          ls -al
          hostname
      - name: Step 2 Checkout
        uses: actions/checkout@v6.1.0

      - name: Step 3 Check
        run: |
          pwd
          ls -al
          hostname
          echo "this is the artifact" >> myfact.txt
          echo "This is an aftifact file to be tested" >> mail.txt
          echo "This will be the 3rd file to be tested in this manner" >> main.txt
#      - name: upload single file 
#        uses: actions/upload-artifact@v7
#        with:
#          name: single-file
#          path: ./myfact.txt

      - name: upload artifact folder
        uses: actions/upload-artifact@v7
        with:
          name: multiple-file
          path: ./ 
          
